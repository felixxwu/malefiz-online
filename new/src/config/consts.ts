export const consts = {
  circleRadius: 35,
  pathStrokeWidth: 5,
  minZoom: 0.1,
  maxZoom: 20,
  zoomSlowdown: 1000,
  panSlowdown: 2,
  playerTransition: 500,
  mapPositionRef: 'mapPositionRef',
}
