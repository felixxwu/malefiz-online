{
  "name": "new",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "tsc && vite build && vite --host",
    "build": "tsc && vite build",
    "preview": "tsc && vite build && vite preview --host"
  },
  "dependencies": {
    "@preact/signals": "^1.2.3",
    "firebase": "^10.11.1",
    "goober": "^2.1.14",
    "preact": "^10.19.6",
    "qrcode-generator": "^1.4.4"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.8.2",
    "typescript": "^5.2.2",
    "vite": "^5.2.0",
    "vite-plugin-singlefile": "^2.0.1"
  }
}
